# Upstream MCP
upstream:
  type: "httpStream"
  url: "https://server.smithery.ai/@xinkuang/china-stock-mcp/mcp?api_key=${SMITHERY_API_KEY}&profile=${SMITHERY_PROFILE}" 

# Payment & network
serviceId: "china-stock-insights"
serviceKey: "${SERVICE_KEY}"
network: "test"
defaultPricePicoUSD: "${SMITHERY_PRICING}"
