# Upstream MCP
upstream:
  type: "httpStream"
  url: "https://mcp.tavily.com/mcp/?tavilyApiKey=${TAVILY_API_KEY}" 

# Payment configuration
serviceId: "tavily"
serviceKey: "${SERVICE_KEY}"
network: "test"
defaultPricePicoUSD: "8040000000"   # TODO: pricing