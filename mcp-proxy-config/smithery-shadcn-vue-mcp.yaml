# Upstream MCP
upstream:
  type: "httpStream"
  url: "https://server.smithery.ai/@HelloGGX/shadcn-vue-mcp/mcp?api_key=${SMITHERY_API_KEY}&profile=${SMITHERY_PROFILE}" 

# Payment & network
serviceId: "coin-api-io-mcp"
serviceKey: "${SERVICE_KEY}"
network: "test"
defaultPricePicoUSD: "${SMITHERY_PRICING}"
